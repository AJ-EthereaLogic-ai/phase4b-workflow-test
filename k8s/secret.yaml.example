# =============================================================================
# Phase4B Test Project - Kubernetes Secrets (EXAMPLE - DO NOT COMMIT)
# =============================================================================
# Template for storing sensitive configuration data
#
# IMPORTANT SECURITY NOTES:
# 1. This is an EXAMPLE file - copy to secret.yaml and fill in real values
# 2. NEVER commit secret.yaml to version control
# 3. Add secret.yaml to .gitignore
# 4. Values must be base64-encoded
# 5. Consider using external secret management (Sealed Secrets, Vault, etc.)
#
# To create base64 values:
#   echo -n "your-api-key-here" | base64
#
# Usage:
#   cp k8s/secret.yaml.example k8s/secret.yaml
#   # Edit secret.yaml with your base64-encoded values
#   kubectl apply -f k8s/secret.yaml -n phase4b_test
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: phase4b_test-secrets
  namespace: phase4b_test
  labels:
    app: phase4b_test
    component: secrets
type: Opaque
data:
  # LLM Provider API Keys (base64-encoded)
  # Replace these placeholder values with your actual base64-encoded API keys
  ANTHROPIC_API_KEY: "c2stYW50LVlPVVItQU5USFJPUElDLUtFWS1IRVJF"  # Replace with: echo -n "sk-ant-..." | base64
  OPENROUTER_API_KEY: "WU9VUi1PUEVOUk9VVEVSLVNFQ1JFVC1IRVJF"  # Replace with: echo -n "..." | base64

  # Monitoring Credentials
  GRAFANA_ADMIN_PASSWORD: "Y2hhbmdlbWU="  # Replace with: echo -n "your-password" | base64

---
# Alternative: Using stringData (automatic base64 encoding)
# Uncomment and use this format if you prefer not to manually base64-encode
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: phase4b_test-secrets
#   namespace: phase4b_test
#   labels:
#     app: phase4b_test
#     component: secrets
# type: Opaque
# stringData:
#   ANTHROPIC_API_KEY: "sk-ant-..."
#   GRAFANA_ADMIN_PASSWORD: "changeme"
